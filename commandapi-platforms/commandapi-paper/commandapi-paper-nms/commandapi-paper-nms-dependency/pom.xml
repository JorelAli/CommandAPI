<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>dev.jorel</groupId>
		<artifactId>commandapi-paper-nms</artifactId>
		<version>9.6.0-SNAPSHOT</version>
	</parent>

	<artifactId>commandapi-paper-nms-dependency</artifactId>
	<packaging>pom</packaging>

	<dependencies>
		<!-- We specifically need to add spigot-mapped nms-common because otherwise the always mojang-mapped paper
		     modules will incorrectly pull in mojang-mapped nms-common -->
		<!-- TODO: Should there be a single paper jar that just loads with the correct mappings that the version has by
		      default? E.g. one jar that loads spigot-mapped on 1.19 or whatever and mojang-mapped on 1.21 and similar.
		      That would mean including both mapped versions of nms-common, which I think is possible through shade
		      relocation. It is kinda weird that we include the always mojang-mapped 1.21 and 1.20.5 nms modules in jars
		      that are supposed to be spigot-mapped. Since the jar has spigot-mapped nms-common classes, they don't
		      work anyway. Even if Paper remaps nms-common to mojang-mappings, remapping the 1.21 and 1.20.5 classes that
		      are already mojang-mapped doesn't work out and the code can't run anyway. Alternatively, should the spigot-mapped
		      paper jars include spigot-mapped versions of 1.20.5 and 1.21? CommandAPIPaper does explicitly double check that
		      its running on Paper, so is running CommandAPIPaper on a Spigot mapped server something that could happen? -->
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-bukkit-nms-common</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-nms-common</artifactId>
			<version>${project.version}</version>
		</dependency>

		<!-- NMS-specific implementations -->
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.21</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.20.5</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.20.3</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.20.2</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.20</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.19.4</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.19.3</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.19.1</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.19</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.18.2</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.18</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.17</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.17.1</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.16.5</artifactId>
			<version>${project.version}</version>
		</dependency>
	</dependencies>

</project>