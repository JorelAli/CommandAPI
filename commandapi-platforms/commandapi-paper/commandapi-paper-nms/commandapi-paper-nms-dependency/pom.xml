<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>dev.jorel</groupId>
		<artifactId>commandapi-paper-nms</artifactId>
		<version>9.6.0-SNAPSHOT</version>
	</parent>

	<artifactId>commandapi-paper-nms-dependency</artifactId>
	<packaging>pom</packaging>

	<dependencies>
		<!-- We specifically need to add spigot-mapped nms-common because otherwise the always mojang-mapped paper
		     modules will incorrectly pull in mojang-mapped nms-common. Even though the dependency reduced poms of
		     those modules have their nms-common dependencies removed (since they reference a relocated version to avoid
		     conflict with the default spigot-mapped version), Maven will only use the dependency-reduced pom if you
		     build the modules separately. This is a 9-year-old Maven bug >:( https://issues.apache.org/jira/browse/MNG-5899.
		     We would like to build everything at once, so we gotta do this. That bug causes other annoying but not detrimental
		     issues, so it would still be nice to redo the build system a bit to find a different solution for that issue. -->
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-bukkit-nms-common</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-nms-common</artifactId>
			<version>${project.version}</version>
		</dependency>

		<!-- NMS-specific implementations -->
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.21</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.20.5</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.20.3</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.20.2</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.20</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.19.4</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.19.3</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.19.1</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.19</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.18.2</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.18</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.17</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.17.1</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>dev.jorel</groupId>
			<artifactId>commandapi-paper-1.16.5</artifactId>
			<version>${project.version}</version>
		</dependency>
	</dependencies>

</project>